import styled from "@emotion/styled";
import DocumentCards from "./components/document/documentCards";

const AppDocumentCards = styled(DocumentCards)({
    width: "80%",
});

export { AppDocumentCards };
